.DS_Store
prof
.obsidian
.replit
replit.nix
.direnv/

# Private directory - structure tracked, content ignored
!private/
!private/README.md
!private/books/
!private/books/**/
!private/books/**/.gitkeep
private/books/**/*.epub
private/books/**/*.pdf
private/books/**/*.mobi
private/books/**/*.azw
private/books/**/*.azw3

# Python cache
__pycache__/
*.pyc

# Reading assistant - separate git repo, fully ignored
reading-assistant/

# Services - each is a separate git repository
services/reading-assistant/
services/admin-api/
services/resume-assistant/

# Website submodule - build artifacts and dependencies
# Note: website/ itself is a submodule, but we need to ignore its build outputs
# when developing locally before full submodule setup
website/node_modules/
website/public/
website/.quartz-cache/
website/tsconfig.tsbuildinfo
